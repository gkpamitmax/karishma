<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For Karishma ❤️</title>

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500&family=Playfair+Display:wght@500&display=swap" rel="stylesheet">

<style>
* { margin:0; padding:0; box-sizing:border-box; }

body {
  font-family: 'Poppins', sans-serif;
  background: #f8f6f3;
  color: #222;
  scroll-behavior: smooth;
}

/* NAV */
nav {
  position: fixed;
  width:100%;
  padding:20px 40px;
  display:flex;
  justify-content: space-between;
  background: rgba(255,255,255,0.6);
  backdrop-filter: blur(10px);
  z-index:100;
}

nav span {
  font-family:'Playfair Display';
  font-size:1.2rem;
}

/* HERO */
.hero {
  height:100vh;
  display:flex;
  justify-content:center;
  align-items:center;
  flex-direction:column;
  text-align:center;
}

.hero h1 {
  font-family:'Playfair Display';
  font-size:3rem;
}

.hero p {
  opacity:0.7;
  margin:15px 0;
}

button {
  padding:12px 28px;
  border:none;
  border-radius:30px;
  background:#222;
  color:white;
  cursor:pointer;
  transition:0.3s;
}

button:hover { background:#555; }

/* PARALLAX */
.parallax {
  height:80vh;
  background:url('https://images.unsplash.com/photo-1501785888041-af3ef285b470') center/cover fixed;
  display:flex;
  align-items:center;
  justify-content:center;
  color:white;
}

.parallax h2 {
  background: rgba(0,0,0,0.4);
  padding:20px 40px;
  border-radius:10px;
}

/* SECTION */
.section {
  padding:100px 10%;
  text-align:center;
}

.section h2 {
  font-family:'Playfair Display';
  margin-bottom:15px;
}

.section p {
  max-width:600px;
  margin:auto;
  opacity:0.7;
  line-height:1.6;
}

/* GALLERY */
.grid {
  display:grid;
  grid-template-columns: repeat(auto-fit, minmax(250px,1fr));
  gap:20px;
  margin-top:40px;
}

.grid img {
  width:100%;
  border-radius:20px;
  transition:0.4s;
}

.grid img:hover {
  transform:scale(1.05);
}

/* RAIN */
.rain {
  background: linear-gradient(#232526,#414345);
  color:white;
  position:relative;
}

.drop {
  position:absolute;
  width:2px;
  height:15px;
  background:white;
  animation:fall linear infinite;
}

@keyframes fall {
  to { transform: translateY(100vh); }
}

/* PROPOSAL */
.proposal {
  padding:120px 20px;
  text-align:center;
}

.proposal h1 {
  font-family:'Playfair Display';
  font-size:2rem;
  max-width:700px;
  margin:auto;
  line-height:1.6;
}

.yes {
  background:#ff4d6d;
}

.no {
  position:absolute;
  background:#ddd;
}

/* MODAL */
.modal {
  position:fixed;
  top:0; left:0;
  width:100%;
  height:100%;
  display:none;
  justify-content:center;
  align-items:center;
  background:rgba(0,0,0,0.6);
}

.modal-box {
  background:white;
  padding:30px;
  border-radius:20px;
  max-width:420px;
  text-align:left;
  line-height:1.6;
}

/* FADE ANIMATION */
.fade {
  opacity:0;
  transform:translateY(20px);
  transition:1s;
}

.fade.show {
  opacity:1;
  transform:translateY(0);
}

</style>
</head>

<body>

<nav>
  <span>For Karishma ❤️</span>
</nav>

<!-- HERO -->
<section class="hero fade">
  <h1>Some things are designed…</h1>
  <p>and some just feel like they were meant to be.</p>
  <button onclick="scrollNext()">Start</button>
</section>

<!-- MOUNTAIN -->
<section class="parallax fade">
  <h2>
  Karishma…  
  I think you love mountains because they’re calm, grounded…  
  and quietly beautiful — just like you.
  </h2>
</section>

<!-- GALLERY -->
<section class="section fade">
  <h2>The Way I See You</h2>
  <p>
  There’s something about you…  
  the simplicity, the warmth, the peace you carry —  
  it stays with me.
  </p>

  <div class="grid">
    <!-- REPLACE THESE -->
    <img src="IMAGE1.jpg">
    <img src="IMAGE2.jpg">
    <img src="IMAGE3.jpg">
  </div>
</section>

<!-- RAIN -->
<section class="section rain fade" id="rain">
  <h2>Little Moments</h2>
  <p>
  Somewhere, someday…  
  I imagine us sitting together, sipping chai,  
  listening to the rain… doing nothing, yet feeling everything.
  </p>
  <button onclick="toggleRain()">Feel the Rain 🌧</button>
</section>

<!-- SPIRITUAL -->
<section class="section fade">
  <h2>Something I Feel</h2>
  <p>
  I don’t know how to explain it…  
  but being around you just feels right.  

  Like you’re not just someone I met…  
  but someone my soul recognizes.
  </p>

  <button onclick="openLetter()">Read More</button>
</section>

<!-- FINAL -->
<section class="proposal fade">
  <h1>
  I don’t know where life takes us…  

  but if it feels like this — calm, real, and meaningful…  

  I’d quietly choose to walk that path… with you. ❤️
  </h1>

  <br>
  <button class="yes" onclick="yesClicked()">😊</button>
  <button class="no" id="noBtn">😅</button>
</section>

<!-- LETTER -->
<div class="modal" id="modal">
  <div class="modal-box">
    <h3>For You, Karishma ❤️</h3>
    <p>
    I don’t know exactly when it started…  
    but somewhere along the way, you became really special to me.  

    With you, I feel lighter… happier… more alive.  
    Like life just makes more sense.  

    It’s not just about liking you…  
    it’s about the way I feel when I’m around you.  

    And maybe this sounds simple…  
    but I truly wish to be around you —  
    today, tomorrow, and for a long time ahead.  

    Because somewhere deep down…  
    it feels like you’re not just a part of my life…  
    you feel like the reason behind my happiness.
    </p>
    <br>
    <button onclick="closeLetter()">Close</button>
  </div>
</div>

<script>
/* SCROLL */
function scrollNext(){
  window.scrollTo({top: window.innerHeight, behavior:'smooth'});
}

/* FADE IN */
const fades = document.querySelectorAll('.fade');
window.addEventListener('scroll', () => {
  fades.forEach(el => {
    const top = el.getBoundingClientRect().top;
    if(top < window.innerHeight - 100){
      el.classList.add('show');
    }
  });
});

/* RAIN */
let rainOn = false;
function toggleRain(){
  rainOn = !rainOn;
  if(rainOn){
    for(let i=0;i<80;i++){
      let d = document.createElement("div");
      d.className="drop";
      d.style.left=Math.random()*100+"vw";
      d.style.animationDuration=(Math.random()+0.5)+"s";
      document.getElementById("rain").appendChild(d);
    }
  } else {
    document.querySelectorAll(".drop").forEach(e=>e.remove());
  }
}

/* LETTER */
function openLetter(){
  document.getElementById("modal").style.display="flex";
}
function closeLetter(){
  document.getElementById("modal").style.display="none";
}

/* BUTTONS */
function yesClicked(){
  alert("That makes me really happy ❤️");
}

const noBtn = document.getElementById("noBtn");
noBtn.addEventListener("mouseover", () => {
  noBtn.style.left=Math.random()*window.innerWidth+"px";
  noBtn.style.top=Math.random()*window.innerHeight+"px";
});
</script>

</body>
</html>